# ** imports **
from zope.component import getUtility
from plone.i18n.normalizer.interfaces import IIDNormalizer

# ** code **
normalizer = getUtility(IIDNormalizer)
new_id = normalizer.normalize(${id})